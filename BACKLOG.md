# BACKLOG.md — deferred findings and revisits

Items reviewers or the developer accepted "for now" with a concrete revisit
trigger. Reviewed at every phase exit (alongside the coherence audit); an
item whose trigger has arrived is either done in that phase or re-deferred
here with a new trigger — never silently dropped.

| Item | Source | Trigger |
|---|---|---|
| ~~SHA-pin GitHub Actions (`actions/checkout`, `astral-sh/setup-uv`) instead of mutable tags~~ **DONE Phase 3** — both actions pinned to 40-char SHAs (checkout `11d5960`, setup-uv `d4b2f3b`) with the tag kept in a trailing comment, in both CI jobs | security review, Phase 0 | Before the Phase 3 CI integration job lands — resolved |
| ~~Digest-pin compose images (`image@sha256:...`) — full-version tags are still mutable on registry re-push~~ **DONE Phase 3** — all 5 images pinned `name:tag@sha256:...` (redpanda, clickhouse, prometheus, alertmanager, grafana); `make down && make up` verified health-green on the digests | security review, Phase 0 | Together with the Actions SHA-pinning, before Phase 3 — resolved |
| 127.0.0.1 binding still admits any local process to passwordless ClickHouse / Grafana admin | security review, Phase 0 | Only if the stack ever runs on a shared/multi-user host — fine for single-dev laptops |
| ~~Schema-registry subjects have no compatibility mode set; re-registering changed models under default BACKWARD can 409 and fail the seed~~ **DONE Phase 2** — `producer.schemas` sets every subject to `NONE` before posting (`set_compatibility`); verified against Redpanda | coherence audit, Phase 1 | Phase 2 start — resolved |
| ~~Phase 3 integration test must assert against ReplacingMergeTree FINAL state, not raw emitted stream (tiny carries duplicates; see DECISIONS.md Phase 1)~~ **DONE Phase 3** — `tests/integration/test_engine.py` compares `attributed_conversions FINAL` (via `read_attributed_decisions`) and `exposures_landed FINAL` count, never the raw emitted stream | coherence audit, Phase 1 | Phase 3 start — resolved |
| ~~`min(1.0, rate)` co-view clamp in generate.py saturates silently; a fault profile needing the multiplier observable at high caused_rate needs it revisited~~ **DISPOSITIONED Phase 8 (no code change)** — `co_view_bug` keeps `caused_conversion_rate × multiplier = 0.2 × 4.0 = 0.8 < 1.0`, so the sports skew (0.768 caused/exposure vs ≤0.276 elsewhere) is observable BELOW the clamp; the clamp is left as-is (a realism guard, not a bug). Pinned in `test_fault_profiles.py::test_co_view_bug_skews_one_genre_below_saturation`. No fault profile drives the multiplier into saturation, so the clamp never hides a signal | code review, Phase 1 | When authoring the co-view-multiplier-bug fault profile (Phase 8) — resolved |
| ~~Unknown `u-` device ids never repeat, so they exercise IP fallback but can never exercise device-keyed dedup or state; no Phase 5 test may conflate "unknown device" with "duplicate"~~ **DONE Phase 5** — dedup keys on `conversion_id`/`(conversion_id, household_id)`; `tests/test_dedup.py::test_unknown_device_conversions_are_not_duplicates` asserts distinct `u-`-device conversions are not suppressed | developer note, Phase 1 | Phase 5 — resolved |
| The fixture-coverage test pins exact tiny counts (38/12/5 resolve cases, fan-out shapes {2:4, 3:1}) — the pin is load-bearing: re-tuning tiny means changing that test AND the frozen fixtures (now incl. `fixtures/tiny/expected/conversions_resolved.jsonl` and `tests/test_resolve_fixture.py`, which also pins 68 raw rows) together as one deliberate change, never silently. **Phase 16 note:** `expected/attributed.jsonl` was re-frozen once with sign-off (5 rows — the ambiguous deferral; DECISIONS Phase 16); producer output and `conversions_resolved.jsonl` unchanged, so these pins did not move | developer note, Phase 1 (extended Phase 2) | If Phase 2+ ever needs to re-tune the tiny profile |
| Resolve loads the device graph once at startup (`load_graph_index`); a device added to the graph after startup resolves as unknown-device → IP fallback or unresolvable instead of a device hit. Fine for the batch pass (static graph); must refresh on `device_graph` updates under continuous follow. **Re-deferred Phase 16** (trigger fired: resolve is now an in-process step of the engine, so "resolve moves to continuous follow" == the engine does; one refresh story covered both until Phase 17 — since then reconciliation reads NO graph (it explodes the persisted `candidate_households`), so the refresh story is the engine's alone) | architect review, Phase 2 | The Phase-18+ framework decision (continuous follow: Bytewax proper vs Flink) |
| The engine drains `conversions` (and `exposures`) from `OFFSET_BEGINNING` every pass with `enable.auto.commit=False`, ignoring group offsets — correct for the one-shot batch pass, but reprocesses the whole topic on every pass under continuous `make run`. The graph-refresh row covers the graph side, not this offset side. **Re-deferred Phase 16** (trigger fired: the resolve consumer is gone; the drain is now the engine's, `common.kafka.drain`) | coherence audit, Phase 2 | The Phase-18+ framework decision (continuous follow: Bytewax proper vs Flink) |
| ~~The shared-IP fault profile must ENGINEER at least one caused, ambiguously-resolved conversion whose wrong-household candidate has a more-recent in-window exposure than its true household, so the most-recent-exposure reduction actually misattributes it. Verify with an assertion that recall(household) < 1.0 (≥1 caused misattribution) — the fault must be observed, not assumed~~ **DONE Phase 8** — `shared_ip_spike` (seed 0, found by offline oracle search) misattributes **11 caused conversions to a wrong shared-IP household**, 0 caused misses (the entire recall gap 0.8625 = 69/80 is wrong-household). Observed offline (`test_fault_profiles.py::test_shared_ip_spike_misattributes_to_wrong_household`) AND live (`test_context.py::test_shared_ip_fault_is_observed_live`, `make eval` caused_wrong_household=11) | Phase 4 eval finding | Phase 8 — shared-IP fault profile — resolved |
| ~~Phase 5 must create `medium` AND pin a clean-run baseline; the late-injector must stay within allowed lateness or recall can't match until reconciliation~~ **DONE Phase 5** — `medium` (seed 11) built; "clean run" is the non-evicting robustness oracle, and engine == oracle byte-identical (`test_medium_parity.py`); `late.max 300 ≤ allowed_lateness 360`, so no in-tolerance drops (recall 1.0) | Phase 4 coherence audit (Q4) | Phase 5 — resolved |
| Phase 4 checkpoint honesty boundary: RESULTS/README must state plainly what this checkpoint SHOWS (correct cross-device attribution, accuracy scored vs truth, four advertiser metrics) and what it does NOT yet show (shared-IP misattribution + agent triage — Phase 8/9). Risk: a green Phase-4 checkpoint read/screenshotted as if it proved the fault-diagnosis story the project exists for. Bake the honesty-boundary sentence in — don't move Phase 8 earlier. **Phase-7 observability extension (fold in here, same obligation):** the observability/RESULTS write-up MUST carry two sentences so a green Phase-7 checkpoint doesn't lie by omission — (a) "these four alerts detect **operational** faults (lag, watermark stall, match-rate, restatement), NOT attribution inflation; catching plausible-but-wrong numbers is the Phase-9 agent's job" (ARCHITECTURE §4 — the exact reason the agent earns its place); (b) describe the alerts as "each fires on a real knobbed profile and **discriminates** (fires on long_delay, silent on tiny)", NEVER implying single-knob isolation (one knob → one alert) — a stronger claim DONE doesn't require and the fixtures don't prove. **DONE Phase 11** — both halves baked into the docs. (a) operational-not-inflation + the SHOWS/does-not-show checkpoint boundary: README agent section (`README.md`, "the agent does the cross-signal reasoning… catching plausible-but-wrong numbers") + the RESULTS accuracy notes (wrong-household is a fault-profile story, not the clean runs) + the new RESULTS "Observability — alert rules" honesty boundary ("these four detect *operational* faults… not attribution inflation… is the agent's job"). (b) discriminate-not-isolate: same RESULTS "Observability — alert rules" section — the four rules are `make test-alerts`/promtool-proven against real captured registries, fire on `long_delay`, silent on `tiny`, and the write-up states plainly they "discriminate; they do not isolate" (long_delay trips more than one rule; no one-knob→one-alert claim) | Phase 4 coherence audit (Q3); Phase 7 coherence audit (shared-IP boundary + knob-granularity) | When writing RESULTS / README (Phase 11, or any earlier checkpoint write-up incl. the Phase-7 observability section) — resolved |
| ~~**Phase 6 needs a long-delay profile before its Done-when can fire.** `medium` keeps every conversion hot-path-attributable, so it generates zero reconciliation candidates; Phase 6 needs a profile whose causal exposure sits 7d–90d before the conversion~~ **DONE Phase 6** — `producer/profiles/long_delay.json` (seed 6, `conversion_delay_minutes [10, 30240]` straddling 7d/90d) yields 29 caused hot-misses recovered on the reconciliation pass (recall 0.587→0.973) + 3 organics left unmatched; Done-when fires (`make test-int-long-delay`) | Phase 5 coherence audit (Q4) | Phase 6 — resolved |
| **Re-read Phase 16 — it got smaller** (the `conversion_id`-keyed reduce is deleted; the leaf split into `last_touch` + the hot rule; `one_row_per_conversion` added): 287 → ~300 lines with docstrings, one fewer concept. Still one file, still shared by oracle, engine driver and reconciliation. `streaming/attribute.py` now holds the pure leaves, the streaming/watermark/eviction operator, dedup helpers, `StreamState`, and `Candidate`. Kept as one file so oracle and engine share ONE implementation (the invariant that makes parity a real check) — do NOT split preemptively. Revisit if Phase 6 reconciliation adds substantially to it: then split leaf decisions (`attribute_core`) from the windowing operator (window/eviction), preserving the shared-core invariant. **Not triggered Phase 6** — reconciliation is a new *consumer* of `attribute_household` in `reconcile/`; it added no leaf logic to `attribute.py`. Re-deferred, trigger unchanged | Phase 5 coherence audit (Q2) | If a later phase grows `streaming/attribute.py` substantially |
| ~~`engine_join_state_size` is a **monotone high-water** gauge — it records the peak, it does not fall on a live scrape. A Grafana dashboard wants a **current-occupancy** gauge that genuinely rises and falls~~ **DONE Phase 7** — added `engine_join_state_current`, set per household to its post-eviction retained count (`observe_state`), so it rises AND falls across the run (unlike the monotone peak, kept as the scaling high-water); on the Phase-7 dashboard as "current vs peak". Test `test_join_state_current_rises_and_falls_unlike_the_peak` | Phase 5 code review | Phase 7 — resolved |
| ~~Co-view read-time factor deferred from report v1 (Phase 4).~~ **WON'T-DO Phase 10** (DECISIONS Phase 10, Ruling A) — the recorded HARD STOP fired: the Phase-10 near-miss demo is a shared-IP/device-graph discrimination (`ip_resolved_fraction` + candidate counts), NOT a genre number, so nothing in Phase 10 consumes a genre-adjusted advertiser value (row 26's "near-miss needs it" anchor was mis-specified — that's *why* the stop fires). Building it was rejected on the merits: the honest per-genre expected baseline does not exist in serving data (~7% margin), so a positive co-view diagnosis would require feeding the baseline from the producer multiplier (the reporting-reads-generation-params coupling this row forbids). Co-view stays a producer-realism knob; `co_view_bug` is scored as a labeled capability-boundary abstention. Not carried into Phase 11 as open. It stays a read-time reporting factor — never storage, never the rollups (per DECISIONS Phase 1) — so deferring it does not constrain rollup design. When it lands, the factor MUST be a reporting-side config input decoupled from the producer profile (even if it numerically matches in the synthetic setup), so reporting never reads generation parameters. **Re-anchored Phase 7** (the "Phase 7 observability" clause was a calendar hard-stop, not a functional dependency — Phase-7 reporting work was bench latency, not metric semantics; no Phase-7 alert is genre-adjusted; rollups already built, so unused config would just rot). Do NOT anchor to Phase 8 (that couples reporting to a producer fault profile — the exact coupling this row forbids; Phase 8's co-view work is the producer-side multiplier-bug fault, row 15) | Phase 4 report v1 | **When an agent phase (9/10) first needs a genre-adjusted advertiser number — concretely the Phase-10 near-miss demo (real-lift vs shared-IP). HARD STOP: if Phase 10 ships without consuming it, close as a DECISIONS "won't-do" (co-view stays a producer-realism knob, not a reporting factor); do NOT carry into Phase 11 docs as open. Surviving invariant if it lands: reporting-side config input, decoupled from producer profile params** |
| **Live Alertmanager firing path** (Pushgateway or node_exporter textfile collector, per-run gateway reset) — Phase 7 proves the alert rules with promtool against real captured registries but does NOT wire a live scrape → notification (the batch stages exit before a scrape). A real knob → metric → alert-fires-in-Alertmanager chain needs the push path. Same premise defers the dashboards' live rendering: Grafana panels (incl. `engine_join_state_current`, which is dashboard-only, off every gate) populate ONLY under this push path — so "assert the live engine emits the gauge into a scraped series" is subsumed here, not a separate obligation | Phase 7 alert decision | **RE-DEFERRED Phase 9** — Phase 9 built the webhook ENDPOINT (`agent/webhook.py`, POST /alerts, parses the Alertmanager payload, triggers a sweep; unit-tested with a mocked sweep) but NOT the live scrape → Alertmanager → webhook push chain (batch stages still exit before a scrape). The scheduled-sweep trigger is `make agent-run`. Trigger unchanged: build the push path when a phase wants the agent fired by a real Alertmanager webhook, scoped to stage-local alerts |
| **`make bench` equality is empirically-true-on-long_delay, NOT a structural guarantee.** The bench asserts the optimized `campaign_hourly` rollup returns the same metric rows as the naive `report.sql` scan on the profile it runs (long_delay, in CI). That proves equality on *that data*, not that the rollup refresh (`reconcile/rollup.py` `_REFRESH_CAMPAIGN_HOURLY`) is structurally identical to `report.sql`'s filter/join for *every* profile. Wrong-household (shared-IP) rows are where the two could diverge — do NOT read the green as a general guarantee. **RE-DEFERRED Phase 8** — `make bench` is NOT run on any fault profile this phase (not in the Phase-8 DONE command). Structural spot-check for the shared-IP worry: a wrong-household attributed row still carries a real `exposure_id` present in `exposures_landed`, so both the rollup refresh and `report.sql` count it identically via the same `exposure_id` join — no divergence introduced by wrong-household rows. Trigger unchanged: verify structurally OR add a bench run before `make bench` is ever run on a fault profile | Phase 7 developer review | Before `make bench` is run on any new profile, esp. the shared-IP fault profile — verify the refresh semantics structurally first, or add a bench run on that profile and confirm equality |
| ~~**`make bench` direction guard.** The 6dp row-equality assert (`bench.py`) does NOT catch the failure functionality-tester named: `bench.sql` silently stops reading the rollup (e.g. reverts to scanning the raw tables) yet still returns equal rows — equality passes, the optimization is gone.~~ **DONE (fix/bench-direction-guard, 2026-08-20)** — added the magnitude-free direction assert in `queries/bench.py` `run()` (after the equality gate): `if optimized["read_rows"] >= naive["read_rows"]: raise AssertionError(...)`. Pins the optimization *direction* (the actual claim), pins no tunable number (no honesty smell). **The guard exposed a latent non-determinism it did not create** (developer ruling): `read_rows` on a `FINAL` scan counts un-merged ReplacingMergeTree version-parts, so both `campaign_hourly` (a copy per rollup refresh) and `attributed_conversions` (a version per reconciled conversion) made the measured read-size drift with merge timing — CI ran bench right after `test_reconcile.py`'s two extra refreshes and the rollup measured 1020 rows, printing 0.8× (rollup reading MORE). Fix (fuller version, ruled required): `_canonicalize` runs `OPTIMIZE TABLE ... FINAL` on all three read tables before measuring, so `read_rows` reflects merged steady state on BOTH sides — deterministic, re-run-identical, apples-to-apples. Honesty consequence recorded: the both-merged byte win is 1.2× (was 1.6× un-merged); rows still 2.5× (340 < 835). RESULTS updated, ARCHITECTURE §8 gotcha + DECISIONS post-Phase-11 note added. Not the round-logic unit test first proposed — `_round_row` isn't where the risk is | Phase 7 developer review | Next `bench.py` touch — resolved |
| ~~**Prompt-injection surface in the context (SN1).** `AttributionContext` string fields — `ip`, `program_genre`, `campaign_id`, and the top-cluster IPs — are producer/attacker-influenceable data. When Phase 9 assembles the LLM prompt, these MUST reach the model as delimited/structured data, never spliced into instruction text~~ **DONE Phase 9** — the context reaches the model as a fenced ```json DATA block in a user message, explicitly labelled "not instructions" (`agent/loop.py::context_message`); the system prompt carries no context values. `tests/test_prompt_injection.py` asserts a crafted `ip` value lands only inside the fence and never in the instruction text. The webhook adds a second boundary (alert payload is trigger-only, never fed to the LLM) | Phase 8 security review | Phase-9 prompt authoring — resolved |
| ~~**No cross-profile live determinism pin (FG2).**~~ **RESOLVED Phase 10** — the `make agent-eval` sweep runs every profile live and captures each one's deterministic context headline (`match_rate`, `ip_resolved_fraction`, ambiguous/candidate counts, restatement Δ, window-edge share) into a dedicated **per-profile context-headline table** in `docs/RESULTS.md` — all six scenarios, not just the near-miss pair (`run_eval.headline` + `tables.headline_table`, rendered by `render_section`; the near-miss table additionally cross-shows real_lift vs shared_ip). So each profile's live headline is durably pinned there rather than in four more isolated `make test-int-<profile>` stacks. `shared_ip_spike` is pinned live (`test_context.py`); the other four fault profiles' live numbers float (only their offline oracle numbers are pinned in `test_fault_profiles.py`). Low priority — offline determinism is already guaranteed per profile. Natural home is Phase 10, which runs every profile live for the fault→diagnosis table: pin each profile's live headline there rather than adding four more isolated `make test-int-<profile>` stacks now | Phase 8 functionality-tester | Phase 10 (agent eval runs every profile live) — pin live headlines there |
| **Webhook sweep amplification.** `agent/webhook.py` triggers one sweep per FIRING alert in the payload's `alerts[]`. Fine for Phase 9 (LLM mocked, live push deferred), but once the live push lands, N alerts in one webhook → N LLM sweeps (token amplification), and duplicate/flapping alerts re-fire it. Dedupe/bound sweeps per webhook (by `alertname`/`fingerprint`, or one sweep per webhook regardless of alert count — the sweep re-observes the whole DB anyway) BEFORE wiring the live push | Phase 9 developer review | Before wiring the live Alertmanager push path (same trigger as the live-firing-path row) — dedupe/bound sweeps first |
| **Promote the token-free `agent_ro` write-denied check to PR CI.** `test-int-agent` proves the SELECT-only guarantee live (INSERT/ALTER/DROP/CREATE → ACCESS_DENIED) with no LLM call, so it's security-load-bearing AND CI-safe. It's a local proof now (mirrors the other isolated `test-int-*` targets) because it needs its own clean single-profile stack (shared conversion_id space, DECISIONS Phase 5) — it can't share the tiny `make test-int` job. Consider a dedicated CI job that spins a clean shared_ip_spike stack and runs just `tests/integration/test_agent_readonly.py::test_agent_user_cannot_write`, so the read-only guarantee is enforced on every PR, not only locally | Phase 9 developer review | When adding/rebalancing CI integration jobs (weigh the extra clean-stack cycle's CI minutes against enforcing the security boundary per-PR) |
| ~~**Token targets don't auto-load `.env` — the agent dies at the LLM call unless `ANTHROPIC_API_KEY` is exported.**~~ **DONE (fix/agent-env-load, 2026-08-20)** — the two token targets auto-load `.env` via `uv run --env-file`, guarded and scoped: `ENV_FILE := $(wildcard .env)` / `AGENT_ENV := $(if $(ENV_FILE),--env-file .env,)`, wired into `agent-run` and `agent-eval` ONLY (`Makefile:15-16,109,118`). NOT a global `include .env`/`export`. Guard verified with `make -n`: `.env` present → `--env-file .env`, absent → no flag (falls back to exported env). Precedence verified empirically (a load-bearing claim): an exported `ANTHROPIC_API_KEY` wins over the file (CI/exported key stays authoritative), the file only fills the gap when unset — so `messages.create` authenticates from the key `uv` injects into the child `os.environ`, resolving the `TypeError`. The `$0` mechanism proof (key-in-child + precedence) satisfied the acceptance criterion, so the ~$1.50 live `agent-run` call was declined as de-risking nothing. **Mandatory security-review: PASS, 0 findings** (Makefile-only; no secret echoed, `.env` gitignored/untracked, scope confined to the two targets, precedence safe for CI, no CI path reads `.env`). Was the original workaround the memory records (`set -a; . ./.env; set +a`) | Phase 10 developer review | Before any CI path or a routine new-session need runs the agent live — resolved |
| ~~**Stale `sink.py:2` async-inserts marker.** The `streaming/sink.py` docstring says inserts are "synchronous (async inserts → Phase 7)" — doubly stale: Phase 7 was benchmark+observability, and async inserts were never built.~~ **DONE Phase 14** — `streaming/sink.py` docstring now reads "synchronous; async inserts are a SCALING lever, not built (SCALING.md 50k/500k tiers)". Corrected while `streaming/` was open for the scale-curve probe (the trigger fired) | Phase 11 coherence audit | Next `streaming/` code touch — resolved (Phase 14) |
| ~~**No test guards the docs accuracy table against the integration pins.**~~ **DONE (`fix/docs-accuracy-pin`, 2026-08-20)** — `tests/pins.py` is now the single source of truth (`AccuracyPin` per profile: tiny 35/52, medium 92/130, long_delay 44/75 → 73/75; precision/recall derive). The five suites assert against it (integration `test_eval_report`/`test_engine_hardening`/`test_reconcile`, offline `test_accuracy`/`test_medium_parity`), and `tests/test_docs_accuracy_pins.py` parses the README + RESULTS accuracy TABLES and asserts every cell equals the pin — so a pin change moves the docs-table guard AND the live assertions together. long_delay also gained explicit recall literals (0.587/0.973) in `test_reconcile`. Scope: the guard covers the accuracy TABLES only; numbers restated in doc PROSE stay hand-synced (prose-guard deferred — see row 47). Spec-vs-BACKLOG note surfaced: the pins were inline literals + named module constants, not importable "pin constants" as this row worded it — single-sourced into `tests/pins.py`. README/RESULTS print the headline accuracy numbers (tiny 0.673, medium 0.708, long_delay 0.587→0.973) — the docs front door — which are asserted in the integration tests, but nothing checked the *docs* match those pins, so a future pin change could silently diverge from the docs. | Phase 11 functionality-tester | Next `tests/` touch — resolved |
| **`check_runbook.py` trace check is substring/prose-based.** It is partial-rename-blind (`needle in file_text`, so `_canonicalize` → `_canonicalize_tables` still matches; generic needles like `reported_at` survive if the string appears anywhere in the file), `.md`-anchor-only (a `.py#Lnnn` line-anchor fragment goes unchecked), and its 3 ARCHITECTURE §8 prose needles match anywhere in the file, not scoped to §8; no test guards RUNBOOK.md's six-section structure or the "un-alerted" honesty prose. Latent, not a defect — no offending link/rename exists today, and the coherence-auditor ruled the checker fit-for-purpose (the §8 prose match fails LOUDLY on rewording, the safe direction, and gotcha bullets have no symbol to key on). Harden: rename-aware/specific-signature symbol matching, §8-scoped prose needles, `.py#Lnnn` anchor support, six-section-structure + un-alerted-prose guards. Same shape as row 36 (a docs-guard test deferred to avoid re-triggering the review gate in a docs-only phase). **Sentinel-hole rationale (not theoretical):** the substring blind spot is a superstring-rename (`_canonicalize` → `_canonicalize_tables` passes silently) — exactly the move a refactor makes — and Phases 12–14 touch precisely the cited symbols (Phase 13 reuses `bench.py` `_canonicalize`; Phase 12 factors `reconcile/`'s exposure read behind a source interface). So the trace check rides along as a drift sentinel over the incident-relevant code, mostly failing loudly, but with a gap right where 12/13 exercise it. **Reviewed Phase 13 (trigger fired) — RE-DEFERRED, checker unchanged.** Phase 13 reused `bench.py` `_canonicalize` UNCHANGED (imported, not renamed), so the RUNBOOK citation still resolves; `_measure` gained a backward-compatible `settings=` param (a different symbol, not cited by the runbook). `make check-runbook` run after the `bench.py` change — OK (the sentinel did its job, citation resolves). No superstring-rename occurred, so the substring blind spot was not exercised. **Reviewed Phase 12 (trigger fired) — RE-DEFERRED, checker unchanged.** Phase 12 factored `reconcile/`'s exposure read behind an `ExposureSource` interface: removed `_read_exposures_for` (NOT RUNBOOK-cited) and added `recover()`/`finalize()`, but left the RUNBOOK-cited tz symbols (`_max_ingest`, `reported_at`, `toUnixTimestamp64Milli`) untouched. `make check-runbook` run after the refactor — OK (citations resolve; the sentinel did its job). No superstring-rename of a cited symbol, so the substring blind spot was again not exercised. Harden-the-checker work still deferred | Phase 15 functionality-tester + coherence-auditor | Next `check_runbook.py` / `docs/RUNBOOK.md` touch, or next refactor that renames a cited symbol |
| **Coherence-watch: RUNBOOK incident #1 and RESULTS "Why the numbers moved" must stay single-sourced.** They are adjacent, not duplicating: RUNBOOK owns the incident + root cause + generalization (on-call narrative); RESULTS "Why each change works / Why the numbers moved" owns the numbers + a one-line methodology note (measurement provenance). DRY-for-docs = single-source-per-fact + cross-link (the row-1 link-text fix set the RUNBOOK→RESULTS direction). Flag if either grows the other's narrative (RUNBOOK starts re-deriving the benchmark table, or RESULTS starts telling the incident story). **Honored Phase 13** — the cost-lever writeup went in its own NEW `## Query cost levers` section; it did NOT grow "Why the numbers moved" or re-narrate the FINAL read_rows incident. The one RUNBOOK mention (lever 2a: "the version-part cost RUNBOOK incident #1 describes exists only before the merge") is a single-clause cross-link, not the incident story — the single-source-per-fact + cross-link pattern this row endorses | Phase 15 coherence-auditor (Q2) | Next edit to either `docs/RUNBOOK.md` incident #1 or `docs/RESULTS.md` "Why each change works" — verify they stay single-sourced, don't re-narrate |
| **`producer/seed.py` `produce_all` has no mid-loop `poll`/flush, so it caps at librdkafka's default `queue.buffering.max.messages` (~100k).** Surfaced authoring Phase 13: `bench_large` at 90k exposures overflowed the producer queue (`BufferError: Local: Queue full`) because graph+exposures+conversions exceeded 100k messages before the single `flush(60)`. Sized `bench_large` under the limit (55k exposures, 25k conversions, 2k graph = 82k) rather than touch the shared seed entrypoint (out of scope for this phase). A profile that needs >~100k total messages must add a periodic `producer.poll(0)` (or raise `queue.buffering.max.messages`) inside the `send` loop. **Rescoped Phase 13 review-gate:** the *doc-desync-on-resize* half is closed — `queries/measure_levers.py` now derives the RESULTS size headline (rows, granules, the skip-index probe IP) from `count()`/a deterministic pick on the canonicalized tables, not from `bench_large` literals, so resizing the profile cannot silently desync the regenerated RESULTS block. What remains is purely the producer-buffer ceiling: an actual >~100k resize still hits `BufferError` until the poll/flush lands. No longer gates the doc, only a larger profile | Phase 13 developer note | When any profile needs >~100k total produced messages — add a poll/flush loop to `produce_all` (or bump `queue.buffering.max.messages`); security/determinism-neutral (broker-side idempotence already on). Lightweight sub-check: confirm the count()-derived RESULTS headline still matches after any `bench_large` resize |
| **Graduate `bench.py`'s `_canonicalize`/`_measure`/`_round_row` to a public shared harness module.** `queries/measure_levers.py` imports these underscore-private symbols from `queries/bench.py` (Phase 13) — deliberate single-sourcing of the Phase-7 determinism fix, but a private-symbol import across modules is the signal they should be public. Compounding: `_canonicalize` is now pinned by THREE records (bench.py, RUNBOOK incident #1 cites it by name, BACKLOG 37's check_runbook sentinel guards the rename). NOT done at the Phase-13 gate: it moves a RUNBOOK-cited symbol, so it ripples to the citation + `docs/check_runbook.py` (BACKLOG 37) and must be its own small cleanup with the citation update + `make check-runbook`, not folded into a merge | Phase 13 coherence-auditor (Q2) | Next time a THIRD consumer needs `_canonicalize`/`_measure`, or any deliberate `queries/` harness cleanup — extract to a public module (e.g. `queries/summary.py`), update the RUNBOOK citation + check_runbook, run `make check-runbook` |
| ~~**Dagster `reconciled_conversions` reads ALL candidates per partition and filters by day in memory** (`orchestration/assets.py`) — O(days × all_candidates) across a backfill. Fine at profile scale (long_delay: 15 days since Phase 16 × a few dozen candidates), but a per-day candidate query (WHERE toDate(event_time)=day) or a read-once/group-by-day is the scaling move. Same shape as the reconcile exposure read's "load all household exposures, leaf window-filters" note (a per-key/per-day DB predicate is the SCALING lever). Not a correctness issue — the day filter is exact~~ **DONE Phase 17** — `lake_candidates(day)` reads one day's current rows from the lake with a prunable `event_time >= d and < d+1` range predicate (the review gate caught a first cut using `strftime(...)`, which filters but cannot prune; `tests/test_lake_attributed.py` now pins `Total Files Read` for the day filter), no in-memory filter over all candidates | Phase 12 build | **Phase 17 — resolved** |
| ~~**Iceberg day-partition prune uses a global `min_event_time` (earliest candidate − 90d), not a per-candidate day window.** `IcebergExposureSource` prunes days older than any candidate's window (output-invariant), but within the kept range it still loads all of a household's exposures and lets the leaf window-filter — same as the ClickHouse source. A tighter per-candidate/per-day predicate (and a `campaign_id`-aware prune) is the SCALING lever; today's prune is enough to demonstrate day-partition pruning without risking a dropped in-window match~~ **DONE Phase 17** — the reconcile read is per (day, bucket): `[day − 90d, day + 1d)` × that bucket's households (`recover_day`); DuckDB prunes on both transforms (1 of 8 files, `tests/test_bucketed_reconcile.py`) | Phase 12 build | **Phase 17 — resolved** |
| ~~**`make eval` silently scores a profile/DB mismatch instead of erroring.**~~ **DONE (fix/eval-profile-guard, 2026-08-20)** — `eval_meta` marker (single-row RMT off the golden path, no timestamp) stamped by `clickhouse/write_marker.py` on run/run-hot/metrics-capture (every populate target that leaves a scoreable DB; Phase 17 replaced `lake-land` with `replay-serving`); `accuracy/run.py` asserts `marker == --profile` with a loud `ProfileMismatchError` on missing/mismatch (pure `accuracy/guard.py`, `test_eval_guard.py`); Makefile:128-129 comment twin fixed. Live-proven on a clean tiny stack: `eval PROFILE=tiny` passes (0.6731/1.0000), `eval PROFILE=long_delay` exits loud (exit 2), `make test-int` 11 (gate-0 byte-identical). Original problem + design record retained below. `accuracy.run --profile "$(PROFILE)"` (PROFILE ?= tiny; no "last profile" mechanism) reads `data/truth/<profile>/` and scores it against whatever is in the ClickHouse DB — so a bare `make eval` after seeding a non-tiny profile yields a meaningless number (tiny truth vs long_delay DB → ~0.17), not a loud failure. Surfaced by Phase 12's DONE run. Durable fix: fail loudly when the truth profile does not match the seeded data (e.g. compare a seed/profile marker or conversion_id set), NOT stateful "last profile" auto-detect — explicit `PROFILE=` is more deterministic and matches every other target. The prose/demo wording shipped in a separate fix PR (`fix/eval-demo-profile`, PR #23); this row is the durable guard. **Rider (fold into this row's guard PR):** the `eval`-target comment `Makefile:128-129` still reads "for the last seeded profile" — the code-side twin of the CLAUDE.md prose PR #23 fixed; correct it there (alongside the guard) so the wording is dead repo-wide. **Developer-approved mechanism (marker, NOT subset-check):** a single-row `eval_meta` marker table OFF the golden-compared path (`attributed_conversions`/`exposures_landed` FINAL) so gate-0 stays byte-identical; ReplacingMergeTree keyed on a constant → single-row-replace, replay-idempotent; profile as a string (seed as int if useful), NO rendered datetime — epoch-millis if a timestamp is wanted (this write is in the reconcile/sink neighborhood where the §8 clickhouse-connect tz round-trip bit); written on the populate path so the DB self-describes its profile; eval asserts `marker == --profile`, loud raise on mismatch-or-missing. **Reject the conversion_id-subset check:** ids are numbered `c-NNNNNN` from 0, so a smaller profile's set is a subset of a larger one's — tiny ⊆ long_delay, so "truth ⊆ DB" false-passes the exact original bug (tiny truth vs long_delay DB), a guard that greenlights its own failure | Phase 12 DONE run | Next `accuracy/` touch — resolved |
| ~~**No test pins reconcile determinism over an ACCUMULATED (multi-append) lake, nor count-grain Gate-1 parity.** The func-tester saw byte-identical reconcile output while reading a lake **8 appends deep** (`data/lake` accumulates across runs — see next row), proving dedup-on-read at depth, but only implicitly. Two guards deferred: (1) an integration test that lands the same exposures N times then asserts reconcile output rows are unchanged (pins the dedup-on-read invariant end-to-end, not just the offline `test_re_landing_is_idempotent_on_read` read-level check); (2) a test asserting `uniqExact(exposure_id)` on the lake == `count() FROM exposures_landed FINAL` (closes the literal Gate-1 "row count == exposures produced" wording, which today holds only at the distinct grain). Both are additive tests, no code change~~ **DONE Phase 17** — `test_accumulated_lake_reloads_byte_identically` lands the same run 3 more times and asserts the reloaded serving rows are unchanged; the distinct-grain count (lake distinct exposures == `exposures_landed FINAL` count) is asserted in the parity test | Phase 12 functionality-tester | **Phase 17 — resolved** |
| **`data/lake` accumulates unboundedly across runs — no snapshot expiry / compaction.** `make down` removes compose *volumes* but not the host `data/` dir, and Iceberg landing is append-only, so each `make lake-land` grows `raw.exposures` (func-tester measured 2880 physical rows / 8 snapshots across its runs). Parity is unaffected (dedup-on-read collapses re-sends), but the lake grows without bound and Gate-1's "row count == exposures produced" holds only at the **distinct exposure_id grain (360), not physical rows**. Before any non-demo use: add Iceberg `expire_snapshots`/compaction or a clean-lake step (e.g. fold `data/lake` removal into `make down` or a `make clean-lake`), and pin Gate-1 on the distinct grain. Demo-safe today **PARTLY DONE Phase 17 (OPEN — do not strike: an open trigger)** — `make lake-maintain` (Dagster `lake_maintenance`: compact day partitions + expire snapshots) bounds the LIVE file count and snapshot history (compaction REWRITES a day's data files, row content unchanged — pinned on both raw tables); **disk reclaim needs `remove_orphan_files`, absent in pyiceberg 0.11.1** — expiry is metadata-only and the orphaned Parquet stays (measured: on-disk files GREW 24 → 32 across a compaction+expiry; long_delay 211 → 252), so only `make lake-reset` (one of the three sanctioned destructive paths, `lake/destructive.py`) reclaims today. Pinned by `tests/test_lake_maintenance.py::test_expiry_is_metadata_only_on_this_pyiceberg` so a pyiceberg bump that adds `remove_orphan_files` fails loud and someone finishes the job; per-profile lake roots | Phase 12 functionality-tester | pyiceberg gains `remove_orphan_files` (or the lake moves to a catalog/engine that has it) — wire it into `lake_maintenance`, delete the pin's `pytest.raises`, and close this row |
| **Consider promoting `test-int-lakehouse` to a PR CI job (weigh holistically with row 33).** The lakehouse byte-identical parity proof (ClickHouse-sourced == Iceberg-sourced reconcile; Dagster pass writes the same rows) is local-only today, like every other isolated single-profile proof (`test-int-medium/long-delay/shared-ip/agent`) — each needs its own clean-stack cycle (shared conversion_id space, DECISIONS Phase 5), and CI minutes are the constraint. Not made a CI exception because it isn't more fragile than its siblings, and the highest-risk part — the tz round-trip canary (`test_lake_roundtrip`) that actually bit us — ALREADY runs offline in `make test` on every PR (after the Phase-12 ② fix). A full end-to-end CI job would mainly re-cover lower-risk surface at the cost of a whole clean-stack cycle. Decide alongside row 33 (agent_ro) and the long_delay reconciliation proof, not as a one-off — if clean-stack CI cycles are ever spent on the isolated proofs, weigh lakehouse/long_delay/agent together | Phase 12 coherence-auditor (Q1) | When adding/rebalancing CI integration jobs (same holistic trigger as row 33; the tz canary already gates the fragile part offline) |
| **Docs PROSE accuracy citations bypass the table guard.** `tests/test_docs_accuracy_pins.py` (BACKLOG 36) pins the README/RESULTS accuracy TABLE cells to `tests/pins.py`, but the same numbers restated in prose — `docs/RESULTS.md` "why the numbers moved" (0.587/0.973), the §Lakehouse `0.9733` line, `docs/PHASES.md` (0.673/0.973), the `README.md` reconciliation run-comment (0.587→0.973) — are unreachable by a table parser, so a pin change would leave them stale. Deferred deliberately (coherence-auditor D2, fix/docs-accuracy-pin): a prose-citation guard is brittle (varied phrasing, 4dp `0.9733` vs 3dp, different provenance) and would trip on benign rewording. Options when it lands: a targeted scan of each known citation site, or fold the numbers into a shared snippet the docs include. | fix/docs-accuracy-pin coherence-auditor (D2) | Next docs/accuracy phase, or when a pin actually changes — true up the prose sites in the same change |
| ~~**`make resolve` with the default `SOURCE` passes `--source "fixtures  "` (trailing spaces) and exits 2.**~~ **DONE (`fix/make-resolve-source`, 2026-08-21)** — comment moved to its own line; same PR fixes the sibling Makefile bug the Phase-16 functionality-tester found: `test-int-medium` ran `run-hot` without `PROFILE=medium`, stamping `eval_meta` as tiny over a medium DB so the BACKLOG-43 guard compared tiny to tiny and a bare `make eval` printed nonsense (precision 0.0078) instead of failing. Now `run-hot PROFILE=medium`; live-proven: bare `make eval` after `test-int-medium` exits loud with the profile-mismatch message. `tests/test_makefile.py` guards both with `make -n` (offline): resolve's default `--source` is exact, and every isolated live target populates with its own PROFILE. Original: `Makefile:6` was `SOURCE ?= fixtures  # comment` — make keeps the whitespace before an inline `#` as part of the value; pre-existing since Phase 2 / Phase 5 | Phase 16 build + functionality-tester | resolved |
| **Profiles whose deferred shared-IP conversions carry revenue now restate after `make run` — `shared_ip_spike` (25 deferred) and `co_view_bug` (1); `late_burst`'s single deferral is a revenue-0 site_visit (no ROAS move); `real_lift` / `duplicate_flood` / `no_fault_baseline` have none and do not.** The deferred shared-IP conversions are credited by the reconcile pass, so `report_snapshots` PRE != FINAL there even though state-miss recovery is nil — a restatement the Phase-10 eval catalog never saw (pinned per channel in `test_fault_profiles.py::test_in_window_scenarios_restate_only_through_the_deferral_channel`). Two more Phase-16 consequences ride on this trigger: (a) the agent's `ambiguous_attributed` / `ip_resolved_fraction` are structurally 0 on a hot-only DB and appear only post-reconcile (DECISIONS Phase 16); (b) RESULTS' agent-eval and context-headline tables are stamped "measured Phase 10, pre-Phase-16" with the two `max|Δroas|` cells blanked. `make agent-eval` was NOT re-run in Phase 16 (API tokens, pinned) | Phase 16 build | Next `make agent-eval` run (ask first) — re-validate the 6-scenario catalog, re-capture the context-headline table, restore the blanked cells |
| ~~**Spec drift: `specs/phase-16-simplify-core.md` assumes an `attributed_conversions.reason` column that never existed.**~~ **DONE Phase 16** — developer ruled to add it in-phase: `reason ∈ {ambiguous_ip, state_miss}`, null when attributed; additive DDL migration; sink column; fixture re-frozen once with it (DECISIONS Phase 16) | Phase 16 build | resolved |
| ~~**Reconciliation loads the device graph from the compacted `device_graph` topic** (`load_graph_index`, the engine's loader) to expand ambiguous_ip candidates — correct under Phase 16's "keep `make reconcile-dagster` byte-identical" rule, but it puts a broker dependency on a job Phase 17 wants to run purely off the lake~~ **DONE Phase 17** — superseded by `candidate_households` on the deferred row: `expand_candidates` explodes the persisted set, `load_graph_index` is gone from `reconcile/` and `orchestration/`; no `device_graph` lake table needed | Phase 16 developer review | **Phase 17 — resolved** |
| ~~**Phase 17 spec needs a Phase-16 follow-up edit BEFORE its branch opens (coherence audit F1/F2/F3).** (F1) `bucket(N, household_id)` reconciliation collides with the ambiguous placeholder: a deferred row sits in its placeholder household's bucket while its true candidates hash to other buckets, so a bucket-local join would silently stop recovering deferrals — moving numbers, which Phase 17's central constraint forbids. (F2) reconciliation now loads the device graph from the broker; Phase 17 Done-when 5 promises "no Kafka involvement". (F3) `raw.attributed_conversions` "same columns" now includes `reason`. Developer's direction for the spec edit: add a `candidate_households: list[str]` column on the attributed row (`household_id` nullable when ambiguous — removes the placeholder AND the broker dependency: the engine knows the candidate set at deferral time and should keep it), land `device_graph` as a lake table, and write reconciliation's ambiguous path as "explode each deferred row into one row per candidate household (the existing `expand_candidates`) → bucket-local join → reduce by `conversion_id` across buckets" — fan-out/reduce in batch with the full 90-day picture, where it is cheap and correct. The architectural claim was never "no fan-out"; it is "no fan-out on the hot path". Docs-branch spec edit after Phase 16 merges~~ **DONE Phase 17** — spec amended (D1–D12) as the first commit on the branch; F1 → D2 explode-before-bucket + cross-bucket reduce, F2 → D1 `candidate_households` (no graph, no broker), F3 → D3 19-column contract pinned | Phase 16 coherence audit + developer Q4 | **Phase 17 — resolved** |
| **`engine_join_state_current` is last-household-wins — one arbitrary household's post-eviction occupancy, whichever the engine processed last.** Deterministic per code version (fixed iteration order) but meaningless as a gauge: it moved 38/50/40 → 54/43/41 in the committed SCALING block when Phase 16 replaced Bytewax's grouping with dict-insertion order, with nothing else changing. The block was regenerated (`make scale-curve`) and `tests/test_scale_probe.py` now asserts the block's STRUCTURAL columns reproduce; the gauge column is deliberately unpinned | Phase 16 coherence audit (pass 2) + developer ruling | Phase 18 (alert rules recaptured anyway): replace with an order-invariant statistic — max per-household occupancy — or drop it for the whole-engine total the block already carries; update the Grafana "current vs peak" panel and BACKLOG 25's note |
| **prometheus_client private `_value` accessor is used in shipped code and 8 test files** (`streaming/scale_probe.py` reads `EXPOSURES_EVICTED._value.get()` / `JOIN_STATE_CURRENT._value.get()`; tests reset counters with `._value.set(0)`), while `streaming/metrics.py` states the getter is a private API and `tests/test_dataflow.py` (Phase 16) reads deltas through `REGISTRY.get_sample_value`. Not a correctness issue today (pytest is serial; values are deterministic) — a convention that is asserted in one place and followed in none of the others. Migrate to the public registry read (deltas) and, where a reset is genuinely needed, a per-test `CollectorRegistry` | Phase 16 coherence audit (pass 3) | Phase 18 (metrics/alerts get recaptured there) — or the next `streaming/metrics.py` touch |
| **Money is stored as Float64 end-to-end; the rollup's `toDecimal64(toString(x), 4)` is a bridge, not the destination.** RUNBOOK incident 3: Float64 `sum()` is order-dependent, and converting a Float64 to Decimal truncates the binary value — the exact string path works only because the producer quantizes to cents (`tests/test_money_domain.py`). The durable fix is Decimal64(4) end-to-end: pydantic `Decimal` in `Exposure.spend` / `Conversion.revenue`, `Decimal64(4)` ClickHouse columns, Iceberg `decimal(18,4)`, the 19-column contract, the oracle — a schema-contract change with fixture re-freeze + schema-registry consequences, so not a fix PR | `fix/snapshot-float-determinism` | Phase 18a (it touches the rollup and the serving DDL anyway) |
| **`queries/report.sql` and `queries/bench.sql` sum money in Float64 and `make bench` compares the two at 6 dp** — the "compared floats use a dp gate" half of the incident-3 rule, left as is: on long_delay the naive-vs-rollup gap is ~1e-12 against 5e-7; on `bench_large` magnitudes drift is ~1e-8, so a straddle of the 6-dp boundary is possible (~1 % per value). Either both sides go through the same Decimal sum, or the gate widens with a stated reason | `fix/snapshot-float-determinism` review (code-reviewer #4) | First `make bench` equality red, or when report.sql is next touched |
| **`report_snapshots` is a ReplacingMergeTree with NO version column, so which twin a merge keeps is undefined by construction.** A background merge collapses the passes' twins at an undefined moment (observed both ways: unmerged 15 rows / 6 keys across runs, and a merge 2 ms after an insert), keeping one arbitrarily; it has worked because Decimal-summed money makes the twins identical (RUNBOOK incident 3), not because the table is correct. The always-on two-pass pin is therefore merge-immune (per-key money before == after); the direct twin comparison runs on both tables when twins are still unmerged. Fix: a version column (`reported_at` is the key, so a pass sequence number) or an explicit `order by` that makes the twin choice defined | `fix/snapshot-float-determinism` (functionality-tester) | Phase 18a — it touches the snapshot path for the dirty-set refresh |
| **`tests/test_rollup_decimal.py` scans only module-level `_`-prefixed SQL constants** — an INSERT assembled inside a function body escapes it (proven by mutation). A tripwire, not a proof (the behavioural money pins are the proof). An AST scan of `reconcile/rollup.py` for string literals containing `insert into` would close it | `fix/snapshot-float-determinism` review | When a second writer of a versioned table appears, or rollup.py gains a function-built INSERT |
| **Pre-Phase-17 reconciled rows may carry `event_time`/`ingest_time` shifted by the developer machine's UTC offset.** Phase 6–16's `recover()` inserted recovered rows whose timestamps came from clickhouse-connect's naive-UTC read back through its naive write, which it interprets as LOCAL time (ARCHITECTURE §8, Phase 17; probe: naive `12:02:51` stored as `18:02:51` on MDT). Campaign totals, accuracy pins and snapshots are offset-invariant, CI is UTC, so nothing caught it; the hour-grain `campaign_hourly` buckets of reconciled rows were the exposure. Since Phase 17 the only serving-table writer (`lake/load_serving.py`) inserts tz-aware UTC and the old sink is test-only (`tests/oracle.py`), so no new row can be shifted; historical RESULTS numbers are unaffected (none are hour-grain). Raised, not silently fixed | Phase 17 live parity failure | Developer ruling: a DECISIONS note (done, Phase 17) suffices, or an hour-grain check of a pre-Phase-17 capture |
| ~~**Pre-Phase-17 reconciled rows may carry `event_time`/`ingest_time` shifted by the developer machine's UTC offset.** Phase 6–16's `recover()` inserted recovered rows whose timestamps came from clickhouse-connect's naive-UTC read back through its naive write, which it interprets as LOCAL time (ARCHITECTURE §8, Phase 17; probe: naive `12:02:51` stored as `18:02:51` on MDT). Campaign totals, accuracy pins and snapshots are offset-invariant, CI is UTC, so nothing caught it; the hour-grain `campaign_hourly` buckets of reconciled rows were the exposure. Since Phase 17 the only serving-table writer (`lake/load_serving.py`) inserts tz-aware UTC and the old sink is test-only (`tests/oracle.py`), so no new row can be shifted; historical RESULTS numbers are unaffected (none are hour-grain). Raised, not silently fixed~~ **DONE Phase 17** — RUNBOOK incident 3 (symptom / detection-by-inspection / root cause / fix / generalization: every datetime tz-aware UTC at every I/O boundary) + the offline guard `tests/test_tz_invariance.py` (reconcile write path under TZ=UTC vs non-UTC zones → byte-identical, every client-bound datetime aware UTC) + artifact audit (no doc quotes a reconciled-row timestamp; fixtures are model-serialized; the Phase-12 parity passed because both sides were shifted equally) | Phase 17 live parity failure | **Phase 17 — resolved** (developer ruling: incident + guard, not a note) |
| **Phase 18 spec needs a Phase-17 follow-up edit BEFORE its branch opens (Phase-17 coherence audit D2).** `specs/phase-18-cost-and-ops.md` says "Phase 17 is NOT a dependency" (:7-8) yet two Done-when clauses name the deleted hot-path ClickHouse sink: "Reconciliation and the hot-path sink record the `(campaign_id, hour)` keys they touched" (:49-50) and "The engine sink (`streaming/sink.py`) gains an `async_insert=1` mode" (:66, :120). Since Phase 17 the engine lands to the lake (`lake/land_*`) and the ONE serving-table writer is the Dagster load (`lake/load_serving.py`), which already knows the days/keys it touched (D6) — so the dirty-set recording moves to the loader (simpler), and async inserts belong on the loader's `client.insert`, not on a sink that no longer exists. Also carry forward: whether `reconciled_at` should anchor in the lake rather than ClickHouse `_max_ingest` (Phase-17 coherence Q3; kept in ClickHouse for now — it interacts with the dirty-set design). Also: (a) the Phase-18 DONE command (`specs/phase-18-cost-and-ops.md:32`, `make down && make up && make seed PROFILE=long_delay && make run`) no longer runs as written — it needs `make lake-reset PROFILE=long_delay CONFIRM=yes` after `make down` and `PROFILE=long_delay` on `make run` (the engine binds its lake from `--profile`); (b) the loader-owned dirty set gets its OWN gate (developer ruling, Phase-17 review): after a reconcile pass that restates ≥1 campaign, the set of `(campaign_id, hour)` in `rollup_dirty` must equal the set of keys whose `campaign_hourly` rows differ between pre- and post-refresh FULL rebuilds — the dirty set is the contract between the loader and the rollup, and a wrong set is silently wrong while the full-refresh oracle still passes. Same treatment as the Phase-17 F1–F3 edit: first commit on the Phase-18 branch is the spec amendment, STOP for approval | Phase 17 coherence audit D2 + Q3/Q4 | Before `phase-18-cost-and-ops` opens |
| ~~**A bare `pytest` on a non-tiny stack re-stamps `eval_meta=tiny`.** `tests/integration/test_eval_guard_live.py` (tiny suite) writes the marker on whatever ClickHouse is up, so after `make run PROFILE=long_delay` a bare `uv run pytest` (which the `run-tests` hook makes routine) leaves `eval_meta=tiny` over long_delay rows — the exact shape BACKLOG 43 guards, recreated by the guard's own test. Seen by the Phase-17 functionality-tester. Fix: the integration suite stamps only when it populated, never on any connection (or the tiny suite asserts the marker it finds instead of writing one)~~ **DONE Phase 17 (review gate, round 3)** — `tests/conftest.py` skips every `tests/integration` test unless `CTV_INT=1`, which only the `make test-int*` targets export (pinned in `tests/test_makefile.py`); a bare `pytest` can no longer seed the broker or stamp the marker | Phase 17 review gate (functionality-tester) | **Phase 17 — resolved** |
| **`reconcile.run()` scans the whole lake twice and the Dagster asset reads its day twice** — `candidate_days()` + `lake_candidates()` are each a full `read_current(days=None)`; `reconciled_conversions` calls `lake_candidates(day)` for a metadata count while `recover_day` re-reads the day. Correctness unaffected; it partly undercuts the partition-pruning story. A cost-lever item: measure before touching | Phase 17 review gate (code-reviewer #8) | Phase 18 (cost & ops) — measure rows/files read per pass, then dedupe the reads |
| **`lake/` holds two landers and two readers that are schema-parameterized copies** (`land_exposures`/`land_attributed`, `read_exposures`/`read_attributed`). Eight modules with one role each is a package, not a junk drawer — the smell is the pairs. Rule of three: when a THIRD lake table appears (Phase 18's dirty set may be it), collapse them into one lander/reader driven by a table spec (schema, arrow schema, partition spec, dedup key). Not before | Phase 17 review gate (coherence Q2, developer ruling) | A third Iceberg table in `lake/` |
