"""Attribution-integrity agent (ARCHITECTURE §4). Phase 8 delivers only the
deterministic, LLM-free half: the collectors that build a typed
`AttributionContext` from ClickHouse. The loop, hypothesis catalog, probe
registry, and typed `AttributionFinding` are Phase 9."""
