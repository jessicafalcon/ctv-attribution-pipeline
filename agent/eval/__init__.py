"""Phase-10 agent eval: run every fault profile plus the no-fault baseline,
`EVAL_REPS` times, score each finding against a pure rubric, and render the
fault→diagnosis and near-miss tables (ARCHITECTURE §4.3)."""
